========================================================================
Function declaration with if
========================================================================
fun test() {
    val num = 100;
    if (num == 10) {
        throw num;
    }
}
------------------------------------------------------------------------
[1, 5], [3, 4]
fun test() {...
    val num = 100;
    if (num == 10) {...
        throw num;
    }
}

========================================================================
Method declaration with if
========================================================================
fun Foo.test() {
    val num = 100;
    if (num == 10) {
        throw num;
    }
}
------------------------------------------------------------------------
[1, 5], [3, 4]
fun Foo.test() {...
    val num = 100;
    if (num == 10) {...
        throw num;
    }
}

========================================================================
Struct instance
========================================================================
fun test() {
    Foo {
        foo: 10,
        bar: "",
    }
}
------------------------------------------------------------------------
[1, 5], [2, 4]
fun test() {...
    Foo {...
        foo: 10,
        bar: "",
    }
}

========================================================================
Struct declaration
========================================================================
struct Foo {
    val: int,
    other: string,
}
------------------------------------------------------------------------
[1, 3]
struct Foo {...
    val: int,
    other: string,
}

========================================================================
Enum declaration
========================================================================
enum Color {
    Red = 10,
    Blue = 200 + 100,
}
------------------------------------------------------------------------
[1, 3]
enum Color {...
    Red = 10,
    Blue = 200 + 100,
}

========================================================================
Match expression
========================================================================
fun test() {
    match (foo) {
        10 => return,
        20 => {
            return;
        }
    }
}
------------------------------------------------------------------------
[1, 7], [2, 6], [4, 5]
fun test() {...
    match (foo) {...
        10 => return,
        20 => {...
            return;
        }
    }
}
