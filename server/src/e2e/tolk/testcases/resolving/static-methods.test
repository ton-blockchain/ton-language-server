========================================================================
Struct static method
========================================================================
struct Foo {}

fun Foo.bar() {}

fun test() {
    Foo.<caret>bar();
}
------------------------------------------------------------------------
5:8 -> 2:8 resolved

========================================================================
Struct instance method as static
========================================================================
struct Foo {}

fun Foo.bar(self) {}

fun test() {
    Foo.<caret>bar();
}
------------------------------------------------------------------------
5:8 -> 2:8 resolved

========================================================================
Type alias static method
========================================================================
type Foo = int;

fun Foo.bar() {}

fun test() {
    Foo.<caret>bar();
}
------------------------------------------------------------------------
5:8 -> 2:8 resolved

========================================================================
Builtin type static method
========================================================================
fun int.bar() {}

fun test() {
    int.<caret>bar();
}
------------------------------------------------------------------------
3:8 -> 0:8 resolved

========================================================================
Generic struct static method
========================================================================
struct Generic<T> {}

fun Generic<T>.foo() {}

fun test() {
    Generic<int>.<caret>foo({});
}
------------------------------------------------------------------------
5:17 -> 2:15 resolved

========================================================================
Generic type alias static method
========================================================================
struct Generic<T> {}

type Alias<T> = Generic<T>

fun Alias<T>.foo() {}

fun test() {
    Alias<int>.<caret>foo({});
}
------------------------------------------------------------------------
7:15 -> 4:13 resolved

========================================================================
int8 static method
========================================================================
fun int.calcExactF(self) { return self; }
fun int8.calcExactF(self) { return self * 2; }
fun int8?.calcExactF(self) { return self == null ? 0 : self * 3; }

fun main() {
    int8.<caret>calcExactF(x)
}
------------------------------------------------------------------------
5:9 -> 1:9 resolved

========================================================================
Same name methods for several generic structs
========================================================================
struct Pair {}

fun Pair<A,B>.createFrom<U,V>(first: U, second: V): Pair<A,B> {}

struct Wrapper<T> { item: T; }

fun Wrapper<T>.createFrom<U>(item: U): Wrapper<T> { return {item}; }

fun test18() {
    Wrapper<int?>.<caret>createFrom<int8>;
}
------------------------------------------------------------------------
9:18 -> 6:15 resolved
