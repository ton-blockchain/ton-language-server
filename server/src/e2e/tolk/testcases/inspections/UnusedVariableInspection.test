========================================================================
Unused variable inspection
========================================================================
fun foo() {
    val x = 1;
}
------------------------------------------------------------------------
3 0:4 to 0:7 Function 'foo' is never used (tolk)
3 1:8 to 1:9 Variable 'x' is never used (tolk)

========================================================================
Unused _ variable inspection
========================================================================
fun foo() {
    val _ = 1;
}
------------------------------------------------------------------------
3 0:4 to 0:7 Function 'foo' is never used (tolk)

========================================================================
Unused tuple variable inspection
========================================================================
fun foo() {
    val [x, y] = [1, 2];
}
------------------------------------------------------------------------
3 0:4 to 0:7 Function 'foo' is never used (tolk)
3 1:9 to 1:10 Variable 'x' is never used (tolk)
3 1:12 to 1:13 Variable 'y' is never used (tolk)

========================================================================
Used variable
========================================================================
fun foo() {
    val x = 1;
    dump(x + 2);
}
------------------------------------------------------------------------
3 0:4 to 0:7 Function 'foo' is never used (tolk)

========================================================================
Used variable in deep scope
========================================================================
fun foo() {
    val x = 1;
    if (true) {
        {
            dump(x + 2);
        }
    }
}
------------------------------------------------------------------------
3 0:4 to 0:7 Function 'foo' is never used (tolk)

========================================================================
Multiple unused variables
========================================================================
fun foo() {
    val x = 1;
    val y = 2;
    var z = 3;
    var w = z;
}
------------------------------------------------------------------------
3 0:4 to 0:7 Function 'foo' is never used (tolk)
3 1:8 to 1:9 Variable 'x' is never used (tolk)
3 2:8 to 2:9 Variable 'y' is never used (tolk)
3 4:8 to 4:9 Variable 'w' is never used (tolk)

========================================================================
Tuple variable with redef modifier
========================================================================
fun data(): [int, int] {}

fun foo() {
    var some = 10;
    do {
        val [first, some redef] = data();
    } while (some > 10);
}
------------------------------------------------------------------------
3 2:4 to 2:7 Function 'foo' is never used (tolk)
3 5:13 to 5:18 Variable 'first' is never used (tolk)
