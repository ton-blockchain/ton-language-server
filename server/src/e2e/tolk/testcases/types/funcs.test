========================================================================
Function type
========================================================================
fun getExecutableCode(): (int -> int) {}

fun main() {
    val a = getExecutableCode();
    //! ^ (int) -> int
}
------------------------------------------------------------------------
ok

========================================================================
Function type with parameters
========================================================================
fun getExecutableCode(): (() -> int) {}

fun main() {
    val a = getExecutableCode();
    //! ^ () -> int
}
------------------------------------------------------------------------
ok

========================================================================
Function type with several parameters
========================================================================
fun getExecutableCode(): ((int, slice, bool) -> int) {}

fun main() {
    val a = getExecutableCode();
    //! ^ (int, slice, bool) -> int
}
------------------------------------------------------------------------
ok

========================================================================
Function type call
========================================================================
fun getExecutableCode(): (int -> int) {}

fun main() {
    val a = getExecutableCode();
    val res = a();
    //! ^ int
}
------------------------------------------------------------------------
ok

========================================================================
Function type call with complex return type
========================================================================
fun getExecutableCode(): (int -> [int, bool]) {}

fun main() {
    val a = getExecutableCode();
    val res = a();
    //! ^ [int, bool]
}
------------------------------------------------------------------------
ok

========================================================================
Type of function declaration
========================================================================
fun some(value: int): bool {}

fun main() {
    val a = some;
    //! ^ (int) -> bool
}
------------------------------------------------------------------------
ok

========================================================================
Type of static method declaration
========================================================================
fun int.some(value: [int, int]): (bool, int, bool) {}

fun main() {
    val a = int.some;
    //! ^ ([int, int]) -> (bool, int, bool)
}
------------------------------------------------------------------------
ok

========================================================================
Type of instance method declaration
========================================================================
fun int.some(self, value: [bool, int]): (bool, int, bool) {}

fun main() {
    val a = 10.some;
    //! ^ (int, [bool, int]) -> (bool, int, bool)
}
------------------------------------------------------------------------
ok

========================================================================
Type of get method declaration
========================================================================
get fun some(): (bool, int, bool) {}

fun main() {
    val a = some;
    //! ^ () -> (bool, int, bool)
}
------------------------------------------------------------------------
ok
